<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:layout_marginBottom="7dp"
    android:background="@drawable/bg_child_blocked_app_row"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="10dp">

    <ImageView
        android:id="@+id/imgBlockedAppIcon"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:background="@drawable/bg_circle_primary_soft"
        android:contentDescription="Blocked app"
        android:padding="7dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_block" />

    <TextView
        android:id="@+id/tvBlockedAppName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:text="App name"
        android:textColor="@color/neutral_900"
        android:textSize="13sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_child_blocked_badge"
        android:fontFamily="sans-serif-medium"
        android:paddingHorizontal="7dp"
        android:paddingVertical="4dp"
        android:text="BLOCKED"
        android:textColor="@color/primary_700"
        android:textSize="8sp"
        android:textStyle="bold" />
</LinearLayout>
